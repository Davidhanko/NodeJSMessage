extends layout

block content
  h1= title
  p Welcome to #{title}
  each message in messages
    p
      strong #{message.user}
      | : #{message.text}
      | #{message.added}
  a(href='/new') New message